{
  "name": "@rag-doctor/adapters",
  "version": "0.1.0",
  "description": "External trace format adapters for RAG Doctor — converts LangChain, LangSmith, event-based traces into canonical format",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rag-doctor/types": "workspace:*"
  },
  "devDependencies": {
    "@rag-doctor/ingestion": "workspace:*",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "files": ["dist"]
}
